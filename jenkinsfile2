pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                sh 'chmod +x hello.sh'
                sh './hello.sh'
                sh 'mail has been sent successfully'
                
            }
        }
    }
}

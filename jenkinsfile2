pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                sh 'chmod +x hello.sh'
                sh './hello.sh'
                echo "Mail sent successfully"
                
                
            }
        }
    }
}
